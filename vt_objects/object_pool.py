class ObjectPool:
    
    def __init__(self, working_set_object, current_mask):
        self.working_set_object = working_set_object
        self.current_mask = current_mask
    
    def is_ready():
        """Returns true if the pool is ready for transmission"""
        return False #TODO do it right
    
    def get_data():
        """Get this pool as data to transmit it over ISOBUS."""
        return #TODO return data